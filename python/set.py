#####SET 
# set is muttable 
# sat = {1,2,3,5}
# print("this si my first set:-", sat)
# print("type of my set:- ",type(sat))
# print("length of my set:- ", len(sat))

#sat = {"hello","garima","hemant"}
#sat.remove("garima") # we use it to remove the element of the set 
# print(sat)
# sat.remove(1) # it wil give error because we don't pass index to remove that index element.
#sat.discard(1) # it will not give error even if element is not in set
#sat.discard("hello") # this will give output and remove the element which we passs not index
# print(sat) # and print the set as it 